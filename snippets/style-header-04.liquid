<style>
  /* HEADER STYLES */
  .header-04 {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 20px;
  }

  /* LOGO */
  .logo-wrapper {
    display: flex;
    justify-content: center;
    flex-grow: 1;
  }

  .logo img {
    max-height: 50px;
  }

  /* NAVIGATION MENU (Visible Now) */
  .wrapper-navigation {
    flex-grow: 2;
    display: flex;
    justify-content: center;
  }

  .menu {
    display: flex;
    gap: 20px;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .menu li a {
    text-decoration: none;
    color: black;
    font-size: 14px;
  }

  /* RIGHT HEADER ICONS */
  .right-header {
    display: flex;
    gap: 15px;
    justify-content: flex-end;
    flex-grow: 1;
  }

  .right-header a {
    text-decoration: none;
    font-size: 18px;
  }
</style>
