<style>
  /* HEADER STYLING */
  .header-04 {
    width: 100%;
    position: relative;
    z-index: 10;
  }

  /* LOGO */
  .logo-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-grow: 1;
  }

  .logo img {
    max-height: 60px;
  }

  /* NAVIGATION (Align Right) */
  .wrapper-navigation {
    display: flex;
    justify-content: flex-end !important;
    flex-grow: 1;
  }

  .nav-bar {
    display: flex;
    align-items: center;
    gap: 20px;
  }

  /* RIGHT HEADER (Icons - Search, Account, Cart) */
  .right-header {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 15px;
    flex-grow: 1;
  }

  .search-icon a,
  .account-icon a,
  .cart-icon a {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: inherit;
  }

  .cart-icon .cartCount {
    background-color: red;
    color: white;
    font-size: 12px;
    border-radius: 50%;
    padding: 4px 7px;
    position: absolute;
    top: 0;
    right: 0;
  }

  @media (max-width: 768px) {
    .wrapper-header-bt {
      flex-direction: column;
      align-items: center;
    }

    .wrapper-navigation {
      justify-content: center !important;
    }

    .right-header {
      justify-content: center;
      gap: 10px;
    }
  }
</style>
